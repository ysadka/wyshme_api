# Create relations for 6 boards
<% 6.times do |board_n| %>
# 7 items each
<% 7.times do |n| %>
<% item_n = board_n * 7 + n %>
board_item_<%= "#{ board_n }_#{ item_n }" %>:
  board: board_<%= board_n %>
  item: item_<%= item_n %>
<% end %>
<% end %>